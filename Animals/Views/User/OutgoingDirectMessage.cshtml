@model IEnumerable<Animals.Models.Post>

<div class="p-3">

    @foreach (var i in Model)
    {
        <p class="text-info">
            <i class="fa fa-angle-right"></i>
            @i.Content
        </p>
    }
</div>
